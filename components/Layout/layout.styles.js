import { css } from "@emotion/core";
import styled from "@emotion/styled";

// add more fun layout bits later
export const StyledLayout = styled.main``;

export const GlobalStyles = css`
  body,
  html {
    background: #4b4b4b;
  }
  *,
  *:before,
  *:after {
    box-sizing: border-box;
  }
`;
